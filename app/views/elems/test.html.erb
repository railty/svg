<style>
svg {
	width: 100%; height: 100%;
	pointer-events: none; /* so that you can resize the element */
}

text {
	text-anchor: middle;
	pointer-events: auto; /* Cancel the svg’s pointer-events */
	fill: currentColor;
  dominant-baseline: middle;
}

rect{
  stroke: blue;
  stroke-width: 1;
  fill-opacity: 0;
  stroke-opacity: 0.5;
  vector-effect: non-scaling-stroke;
}

.avoid-clicks {
  pointer-events: none;
}
.border {
  stroke:#006600;
  fill:#cccccc;
}
#selection {
  stroke: blue;
  stroke-width: 1;
  fill-opacity: 0;
  stroke-opacity: 0.5;
  vector-effect: non-scaling-stroke;
}
#canvas{
  cursor: crosshair;
}
#rb{
  cursor: nw-resize;
}

</style>

<div id="example"/>
<script>
var data = {
    mouse: {
      pt: {x: 80, y: 80},
      button: 0,
      draging: false,
    },
    selection: {x:10, y:10, width:10, height:10},
    handles: [{x:80, y:80}],
    ellipse: {cx:140, cy:50, rx:50, ry:120},
    elements: [{tag:'text', x:20, y:40, text:'text 1', scaleX:1, scaleY:1}, {tag:'text', x:10, y:60, text:'text 2', scaleX:2, scaleY:2}]
  };
React.render(React.createElement(Elem, {data: data}), document.getElementById('example'));

</script>
