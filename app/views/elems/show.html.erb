<style>
.avoid-clicks {
  pointer-events: none;
}
.border {
  stroke:#006600;
  fill:#cccccc;
}
#selection {
  stroke: blue;
  stroke-width: 1;
  fill-opacity: 0;
  stroke-opacity: 0.5;
  vector-effect: non-scaling-stroke;
}
</style>

<div id="example"/>
<script>
var data = {
    mouse: {x:80, y:80},
    selection: {x:10, y:10, width:10, height:10},
    handles: [{x:80, y:80}],
    ellipse: {cx:140, cy:50, rx:50, ry:120},
    elements: [{tag:'text', x:20, y:40, text:'text 1'}, {tag:'text', x:10, y:60, text:'text 2'}]
  };
React.render(React.createElement(Svg, {data: data}), document.getElementById('example'));

</script>
