<style>
.avoid-clicks {
  pointer-events: none;
}
.border {
  stroke: black;
  fill: grey;
}
.svg-text{
  fill-opacity: 0;
  stroke: blue;
  stroke-width: 1;
}
#selection rect{
  stroke: blue;
  stroke-width: 1;
  fill-opacity: 0;
  stroke-opacity: 0.5;
  vector-effect: non-scaling-stroke;
}
rect{
  stroke: blue;
  stroke-width: 1;
  fill-opacity: 0;
  stroke-opacity: 0.5;
  vector-effect: non-scaling-stroke;
}

text {
	text-anchor: middle;
	pointer-events: auto;
	fill: currentColor;
  dominant-baseline: middle;
}

#canvas{
  cursor: crosshair;
}
#rb, #lt{
  cursor: nwse-resize;
}
#lb, #rt{
  cursor: nesw-resize;
}
#lm, #mt{
  cursor: move;
}

</style>

<div id="example"/>
<script>
var data = {
    mouse: {
      pt: {x: 80, y: 80},
      button: 0,
      draging: false,
    },
    selection: {x:10, y:10, width:10, height:10},
    handles: [{x:80, y:80}],
    ellipse: {cx:140, cy:50, rx:50, ry:120},
    //elements: [{id:1, tag:'text', x:20, y:40, text:'text 1', transform:"scale(1, 1)"}, {id:2, tag:'text', x:10, y:60, text:'text 2', transform:"scale(1, 1)"}]
    elements: [{id:1, tag:'tb', x:10, y:20, width:80, height:60, text:'fghijk'}]
  };
React.render(React.createElement(Svg, {data: data}), document.getElementById('example'));

</script>
